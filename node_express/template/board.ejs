<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div>테스트!</div>

    <% list.forEach( item =>{ %>
        <li><%= item.boardNo %> 번 || <%= item.title %> || <%= item.contents %></li>
    <% }) %>
    <% list.forEach( item =>{ %>
        <li><%= item.boardNo %> 번 || <%= item.title %> || <%= item.contents %></li>
    <% }) %>
  
    <!-- <% list.forEach(function(item) { %>
        <div>
            <%= item.title %>
        </div>
    <% }) %> -->
</body>
</html>